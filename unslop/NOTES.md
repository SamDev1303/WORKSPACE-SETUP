# unslop — NOTES

What this skill learned in use. Newest first.

## 2026-09-16 — vendored

Vendored from Sam's fork `SamDev1303/WORKSPACE-SETUP` (itself from cursor/plugins pstack), frontmatter as Sam edited it (`disable-model-invocation` dropped so agents apply it unprompted). SkillSpector `--no-llm` scan before install: score 0, SAFE. The canonical workflow block (`references/WORKFLOW.md`) tells every seat to run it over anything a person will read.
