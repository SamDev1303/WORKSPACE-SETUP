# workspace — notes

## 2026-09-16 — created (s147, Sam directive)
- Sam: the WORKSPACE-SETUP fork "should be a skill for any CLI, for anyone to use: you, Gideon, Codex, Antigravity, Mac
  Air. This is our main skill to walk into, walk inside, any workspace." Before this, "walk into a workspace" lived in
  three places none of which a non-Claude CLI could invoke: the Universal Workspace Protocol text inside `koda-plan`,
  `scripts/bootstrap-workspace.sh` (nothing called it), and the WORKFLOW block whose beat 0 named a Claude-only plugin.
- Modes mirror `/org` (one skill, named modes). `end` routes to `workspace-hygiene` / `workspace-deep-clean` rather than
  absorbing them: the exact name `workspace` was free on every surface; `workspace-*` and `gsd-*workspace*` are neighbours,
  not collisions.
- Beat 0 is "GSD when present, otherwise the checklist" so codex/agy/opencode get real steps, not commands they lack.
- The pack copy in `~/Work/WORKSPACE-SETUP` is written by `scripts/sync-workspace-pack.sh`; edits happen here.

- 2026-09-18 (s146 close): shipped in this session — see ~/claudeking.cloud/docs/LOG-2026-09.md and the ops-log comment on MYK-359.
